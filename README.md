# Launch assets

The Product Hunt gallery (1270×760 at 2x) and thumbnail, rendered by `scripts/make_launch_assets.swift` on `main` from the app's own screenshots. Kept on this branch so `main` stays light.
